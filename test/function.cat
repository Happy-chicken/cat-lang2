let pi:int = 1000;

def add(x: int, y: int) -> int {
  return x + y;
}

def add_one(x:ptr<int>) {
  *x = *x + 1;
}

def main() ->int {
  let x = 1;
  add_one(&x);
  let a = add(1, 2);
  return x + a;
}
