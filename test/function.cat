let pi:int = 1000;

def add(x: ref int, y: int) -> int {
  x = x + 1;
  return x + y;
}

def swap(a: ref int, b: ref int) {
  let tmp = a;
  a = b;
  b = tmp;
}

def main() ->int {
  let x = 10;
  let y = 20;
  swap(x, y);
  return add(x, y);
}
